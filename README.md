# React Name Printer

A simple React page that takes a name as input and prints it on button click.

## Setup

1. Run `npm install`
2. Run `npm run dev`
3. Open the local URL shown in the terminal
